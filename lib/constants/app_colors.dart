import 'package:flutter/material.dart';

/// Consolidated App color constants (v2 merged into main)
class AppColors {
  // Primary green colors (from previous AppColorsV2)
  static const Color primary = Color(0xFF107C10); // Microsoft Excel Dark Green
  static const Color primaryLight = Color(0xFF50E6C2);
  static const Color secondary = Color(0xFF06B6D4);

  // Status colors
  static const Color success = Color(0xFF107C10);
  static const Color error = Color(0xFFDC3545);
  static const Color warning = Color(0xFFFFC107);

  // Neutral colors - Light theme
  static const Color textPrimary = Color(0xFF222222);
  static const Color textSecondary = Color(0xFF666666);
  static const Color surfaceLight = Color(0xFFFAFAFA);
  static const Color borderLight = Color(0xFFE0E0E0);

  // Neutral colors - Dark theme
  static const Color textPrimaryDark = Color(0xFFEEEEEE);
  static const Color textSecondaryDark = Color(0xFFAAAAAA);
  static const Color surfaceDark = Color(0xFF1E1E1E);
  static const Color borderDark = Color(0xFF333333);

  // Card colors
  static const Color cardLightBg = Color(0xFFFFFFFF);
  static const Color cardDarkBg = Color(0xFF2D2D2D);

  // Gradients
  // Gradients removed — using solid primary color across app
}
